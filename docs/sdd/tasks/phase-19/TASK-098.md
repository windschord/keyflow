# TASK-098: 設定・About・エラー表示の文言外部化と言語セレクタUI

## 基本情報

| 項目 | 内容 |
| ----- | ------ |
| ID | TASK-098 |
| タイプ | feat（US-016） |
| ステータス | TODO |
| 優先度 | High |
| 見積もり | 90分 |
| 依存タスク | TASK-096 |

## 背景

US-016のREQ-016-003（設定画面からの切り替え・永続化）と、REQ-016-007の残り領域
（設定モーダル・Aboutモーダル・エラー表示）を実装する。

## 実装内容

### 言語セレクタUI（REQ-016-003）

- `components/SettingsModal/index.tsx` のUIセクションに言語セレクタを追加する
- 選択肢は「日本語」「English」の自己表記固定（言語切り替えの影響を受けない）
- 変更時: ui-sliceの `setLanguage` を呼び（即時反映）、`settings:set` で
  `ui.language` を永続化する（既存の設定保存パターンに従う）
- 保存値が `'auto'` の場合、セレクタは現在の解決結果（ja/en）を表示してよいが、
  ユーザーが明示選択するまで `'auto'` を上書きしない実装が望ましい。実装が複雑になる
  場合は「明示選択時に'ja'/'en'を保存する」動作を最低ラインとする

### 文言外部化

- `components/SettingsModal/`（セクション見出し・各設定項目ラベル・選択肢）
- `components/AboutPanel/`（AboutModal含む。見出し・説明文。ライブラリ名・
  ライセンス種別名・Salamanderクレジット等の固有名詞は対象外）
- `App.tsx` ほかのエラーダイアログ・トースト文言（パース失敗・ファイル読み込み失敗等）
- MIDIデバイス関連の表示文言（SettingsModal内のデバイス選択等）

### 注意事項

- 音色表示名（グランドピアノ等）がvoices.ts側に定義されている場合、表示層で翻訳キーへ
  マップする（ドメイン定義のIDは変更しない）
- AboutPanelのクレジット文言はCC-BY表記義務に関わるため原文を維持する

## 実装手順（TDD）

1. SettingsModalの言語セレクタのテストを先に作成
   （変更でsetLanguage+settings:set呼び出し、選択肢が自己表記固定）
2. 失敗確認→実装→通過
3. 文言外部化を領域ごとに実施し、既存テストの回帰を確認
4. 全チェック通過を確認しコミット

## 受入基準

- [ ] 設定画面から言語を切り替えると再起動なしで文言が切り替わる（REQ-016-003）
- [ ] 切り替えた言語が `ui.language` として永続化される
- [ ] SettingsModal・About・エラー表示の日本語ハードコードが解消されている
- [ ] 固有名詞（クレジット・ライブラリ名）は翻訳されず維持されている
- [ ] 全チェック通過

## テスト項目

- [ ] 言語セレクタ変更で `settings:set('ui', {...language})` が呼ばれる
- [ ] 言語セレクタの選択肢が「日本語」「English」の自己表記固定
- [ ] SettingsModal: 言語enで見出しが英語になる
- [ ] エラートースト文言がリソース経由で表示される（formatMessage使用箇所）
